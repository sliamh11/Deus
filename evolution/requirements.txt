# Pinned versions — update periodically with: pip install --upgrade <pkg>
# Deus Evolution Loop dependencies
# Core (always required)
google-genai==1.14.0       # Gemini API (same as memory_indexer.py)
sqlite-vec==0.1.6          # Vector search in SQLite

# Cross-encoder reranker for atom retrieval (two-stage: embeddinggemma recall + cross-encoder precision)
sentence-transformers>=3.0.0

# MCP server (optional — for external agent integration)
mcp==1.28.1
starlette==1.3.1                # CVE-2026-48710 fix — MCP's >=0.27 constraint is too loose

# DSPy optimizer — required. The arm auto-triggers in-process from
# cmd_log_interaction (cli.py:451 -> _maybe_batch_judge -> _maybe_auto_optimize),
# so the interpreter the Node host spawns via EVOLUTION_PYTHON needs it present.
# Upper-bounded deliberately: this arm was dead because of two dspy API details
# (GEPA's reflection_lm assertion, the metric's float|ScoreWithFeedback return
# contract). Both are 3.x behaviour verified against 3.3.1; a 4.x major is free
# to change them again and would fail the same silent way, inside cli.py:174's
# except. Raise the ceiling deliberately, after re-checking those two seams.
dspy>=3.2.0,<4
